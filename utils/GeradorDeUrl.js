const GeradorDeUrl = () => {
    // Substitua esta função pela lógica de geração de URLs da API externa
    const gerarUrlDeImagem = () => {
        // Lógica para gerar a URL da imagem da API externa
        const apiUrl = "https://placekitten.com/200/400";// Substitua pela URL da sua API
        
        return apiUrl;
    };

    return {
        gerarUrlDeImagem,
    };
};

export default GeradorDeUrl;